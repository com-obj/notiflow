runtime: java
env: flex
runtime_config:
  jdk: openjdk8
env_variables:
  SPRING_PROFILES_ACTIVE: "cloud_db"
handlers:
- url: /.*
  script: this field is required, but ignored
manual_scaling: 
  instances: 1