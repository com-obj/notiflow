testEnv: local

#spring.flyway.enabled: false

spring.mail:
   host: localhost
   port: 3025
   username: no-reply@objectify.sk
   password: xxx

spring.datasource:
   platform: postgres
   url: jdbc:postgresql://localhost:25432/nc
   username: nc
   password: ZMss4o9mdBLV

nc:
   functions:
      event-generator:
         soruce-dir: eventsQueue
         file-name: ba_job_post.json
      send-email-message:
         from: no-reply@objectify.skk
      email-templates:
         templates-root-dir: 
            - src/test/resources/message-templates
         messages-dir-and-base-name: file:src/test/resources/message-templates/messages
   flows:
      test-mode.enabled: false

logging.level:
   org.springframework: DEBUG
---
spring.config.activate.on-profile: jenkins_test
testEnv: jenkins
spring.datasource:
  url: jdbc:postgresql://localhost:5432/nc
---
spring.config.activate.on-profile: bitbucket
testEnv: bitbucket
spring.datasource:
  url: jdbc:postgresql://localhost:25432/nc
  
logging.level:
   org.springframework: INFO
---
