testEnv: local

spring.flyway.enabled: false

spring.mail:
   host: localhost
   port: 3025
   username: no-reply@objectify.sk
   password: xxx

spring.datasource:
   platform: postgres
   url: jdbc:postgresql://localhost:25432/nc
   username: nc
   password: ZMss4o9mdBLV

nc:
   functions:
      event-generator:
         soruce-dir: eventsQueue
         file-name: ba_job_post.json
      email-templates:
         templates-root-dir: 
            - src/main/resources/message-templates
            - src/test/resources/message-templates
         messages-dir-and-base-name: classpath:message-templates/messages
      send-email-message:
         from: no-reply@objectify.skk
   flows:
      test-mode.enabled: false

---
spring.config.activate.on-profile: jenkins_test
testEnv: jenkins
spring.datasource:
  url: jdbc:postgresql://localhost:5432/nc
---

