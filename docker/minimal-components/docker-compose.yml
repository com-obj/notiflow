version: '3'

services:
  nc-postgres:
    image: postgres:10
    restart: always
    environment:
      POSTGRES_DB: nc
      POSTGRES_USER: nc
      POSTGRES_PASSWORD: ZMss4o9mdBLV
    expose:
      - 5432
    ports:
      - 25432:5432
      
  rabbitmq:
    image: rabbitmq:3.7-management
    restart: always
    expose:
      - '5672'
      - '15672'
    ports:
      - 5672:5672
      - 15672:15672
